The keyboard.[ch] support for a serial keyboard will change as the 
hardware evolves.

Add O_RDWR support for hpposix.

Test lifPack() in close().

Investigate while lifutils.py reports different total size than the OS.

Test hpposix() write() with lifpack

Make lifpack progress bar save background