In the scripts, get the dependencies from the argv[0] directory.

Fix packing of big disks in c/libhp165x/pack.c

In lifutils.py, support typed files with colon.

See why SYSTEM_ crashes loading PVTEST_ from bigdisk.